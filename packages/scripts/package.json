{
  "name": "@zysk/scripts",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "@zysk/services": "workspace:*",
    "axios": "^1.7.7",
    "commander": "^14.0.0",
    "date-fns": "^4.1.0",
    "kysely": "^0.27.4",
    "pg": "^8.13.0"
  },
  "scripts": {
    "lint": "eslint . --ext .ts --color",
    "lint:fix": "pnpm lint --fix",
    "invoke": "pnpm dlx dotenv-cli -e ../../apps/web/.env.local -- ts-node src/index.ts --"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "ts-node": "^10.9.2"
  }
}
