import type { ColorSchema, PrimaryColor } from "#/styles/theme/types";

export const schemaSettings = {
  primaryColor: "palatinateBlue",
  colorSchema: "light",
} as {
  primaryColor: PrimaryColor;
  colorSchema: ColorSchema;
};
