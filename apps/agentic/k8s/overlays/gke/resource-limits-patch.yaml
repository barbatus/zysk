apiVersion: apps/v1
kind: Deployment
metadata:
  name: agentic-worker
  namespace: agentic
spec:
  template:
    spec:
      containers:
        - name: worker
          resources:
            requests:
              cpu: "1"
              memory: "2Gi"
            limits:
              cpu: "4"
              memory: "4Gi"
